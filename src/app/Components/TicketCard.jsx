'use client'

export default function TicketCard({ticket, addToQueue, inQueue}){
    return(
        <div>
            <h1>Ticket Card</h1>
            <button>Add to My Queue</button>
        </div>
    )
}